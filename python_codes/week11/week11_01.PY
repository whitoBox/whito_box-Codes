#week11_1

class Human: #human 이라는 클래스(자료형)을 선언
    pass

h = Human() # 생성자 Human을 호출
        # 메모리에 Human 타입의 값(인스턴스) 생성됨.
        # 생성한 인스턴스의 참조값을 반환한다.
        # 참조값을 (보통은) 변수에 연결을 해준다.
i = int(1)
print(type(h),type(i))


        
